<h1>コメント（<%= closet.closet_comments.count %>件）</h1>
<table class="table mt-3">
  <tbody>
    <% closet.closet_comments.each do |comment| %>
      <tr style="width:100%">
        <td style="word-break:break-word">
          <strong><%= comment.user.name %></strong> &emsp; <%= comment.created_at.strftime('%Y/%m/%d') %></br >
          <%= comment.comment %>
        </td>
        <td style="width:20%">
          <% if comment.comment != "管理者によって削除されました" %>
            <%= link_to "削除", admin_closet_closet_comment_path(closet.id, comment.id), method: :patch,
            remote: true, "data-confirm" => "本当に削除して宜しいですか？" %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>