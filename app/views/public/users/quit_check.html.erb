<div class="container">
  <div class="row">
    <h1>退会確認</h1>
    <p>本当に退会しますか？</p>
    <%= link_to "退会しない", user_path(@user.id) %>
    <%= link_to "退会する", quit_user_path(@user.id), method: :patch %>
  </div>
</div>